extends ../layout/main

block content
  .container.py-4
    if !event
      .row
        .col-md-8
          h1.fw-bold.text-danger Event tidak ditemukan
          p.text-muted.mb-3
            | Maaf, event yang kamu cari tidak tersedia atau sudah dihapus.
          a.btn.btn-outline-danger(href="/events")
            i.bi.bi-arrow-left.me-1
            | Kembali ke daftar event
    else
      .row.mb-3
        .col-md-8
          h1.fw-bold.text-danger.mb-1= event.title

          if event.status
            span.badge.me-2(
              class= event.status === 'upcoming'
                ? 'bg-success-subtle text-success'
                : (event.status === 'ongoing'
                  ? 'bg-primary-subtle text-primary'
                  : 'bg-secondary-subtle text-secondary')
            )
              if event.status === 'upcoming'
                i.bi.bi-sunrise.me-1
                | Akan Datang
              else if event.status === 'ongoing'
                i.bi.bi-broadcast-pin.me-1
                | Sedang Berlangsung
              else
                i.bi.bi-flag-checkered.me-1
                | Selesai

          if event.sport_name
            span.badge.bg-light.text-secondary.border.ms-1
              i.bi.bi-trophy-fill.me-1
              = event.sport_name

        .col-md-4.text-md-end.mt-3.mt-md-0
          a.btn.btn-outline-secondary.btn-sm.me-2(href="/events")
            i.bi.bi-arrow-left.me-1
            | Kembali
          a.btn.btn-danger.btn-sm(href=`/tickets/${event.id}`)
            i.bi.bi-ticket-perforated.me-1
            | Lihat Tiket

      .row
        .col-lg-8
          .card.mb-3
            .card-body
              h6.text-uppercase.text-muted.mb-2 Jadwal & Lokasi

              if event.start_date_formatted
                p.mb-1
                  i.bi.bi-calendar3.me-1
                  if event.end_date_formatted && event.end_date_formatted !== event.start_date_formatted
                    | #{event.start_date_formatted} - #{event.end_date_formatted}
                  else
                    | #{event.start_date_formatted}
              else
                p.mb-1.text-muted
                  i.bi.bi-calendar3.me-1
                  | Jadwal belum ditentukan

              if event.venue_name
                p.mb-1
                  i.bi.bi-geo-alt-fill.me-1
                  | #{event.venue_name}
              if event.city || event.country
                p.mb-0
                  i.bi.bi-geo.me-1
                  | #{event.city || ''} #{event.country || ''}

          .card
            .card-body
              h6.text-uppercase.text-muted.mb-2 Tentang Event
              if event.description
                p.mb-0= event.description
              else
                p.text-muted.mb-0
                  | Belum ada deskripsi detail untuk event ini.

        .col-lg-4
          .card.mb-3
            .card-body
              h6.text-uppercase.text-muted.mb-2 Informasi Tambahan
              dl.row.mb-0
                if event.organizer
                  dt.col-5.text-muted Penyelenggara
                  dd.col-7= event.organizer
                if event.capacity
                  dt.col-5.text-muted Kapasitas
                  dd.col-7= event.capacity
                if event.registration_url
                  dt.col-5.text-muted Pendaftaran
                  dd.col-7
                    a(href=event.registration_url target="_blank")
                      i.bi.bi-box-arrow-up-right.me-1
                      | Buka Link

          .card
            .card-body
              h6.text-uppercase.text-muted.mb-2 Aksi Cepat
              p.text-muted.mb-3
                | Simpan event ini atau bagikan ke teman tim olahragamu.
              .d-flex.gap-2.flex-wrap
                button.btn.btn-outline-secondary.btn-sm(type="button")
                  i.bi.bi-bookmark-plus.me-1
                  | Simpan
                button.btn.btn-outline-secondary.btn-sm(type="button")
                  i.bi.bi-share.me-1
                  | Bagikan
